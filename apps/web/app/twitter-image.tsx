// Twitter Cards fall back to og:image when no twitter:image is present, but
// this file makes the tag explicit rather than relying on that fallback —
// same generated card, just registered under both file conventions.
export { default, alt, size, contentType } from './opengraph-image';
